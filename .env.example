# 프론트엔드 환경변수 (Vite). 실제 값은 .env.local 에 두고 커밋하지 않는다.

# 백엔드 베이스 URL. 백엔드(server/)가 다른 포트면 지정. 비우면 same-origin.
VITE_API_BASE=http://localhost:8787

# "false" 면 실제 Copilot 백엔드 사용. 미설정/기타값이면 데모(mock) 모드.
VITE_USE_MOCK=true
